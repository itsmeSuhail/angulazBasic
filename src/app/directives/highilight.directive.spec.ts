import { HighilightDirective } from './highilight.directive';

describe('HighilightDirective', () => {
  it('should create an instance', () => {
    const directive = new HighilightDirective();
    expect(directive).toBeTruthy();
  });
});
